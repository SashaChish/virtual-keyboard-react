const initColors = {
  theme: {
    color: '#000000',
    bgColor: '#ffffff',
  },
}

export const colors = (state = initColors, action) => {
  switch (action.type) {
    default:
      return state
  }
}
